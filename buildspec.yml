version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
  build:
    commands:
      - echo "Build started on `date`"
      - echo "Preparing artifacts..."
artifacts:
  files:
    - '**/*'